<%= render partial: 'layouts/support_navbar' %>

<div class="container pad20">
	<% @events.each do |event| %>
		
		
			<div class="row pad5">
				<div class="col-12">
					<%= event %>
				</div>	
			</div>
		

	<% end %>
</div>

