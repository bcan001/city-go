<nav class="navbar navbar-expand-lg navbar-light bg-light <%= fetch_venue_image({}) %>" id="venue-navbar">
  <div class="container">
    <div class="row">
      <div class="col-12">
        
		    <div class='venue-title'>
		    	<%= @venue.name %>
		    </div>

		    
		    

		    <div class="venue-text pad10-top pad10-bottom">
					<%= @venue.description %>
				</div>

				<div class="venue-text pad10-top pad10-bottom">
					<%= @venue.address %>
				</div>

				<div class="venue-text pad10-top pad10-bottom">
					<%= @venue.city.name %>, <%= @venue.city.state %>
				</div>

				<div class="venue-text pad10-top pad10-bottom">
					<% @venue.links.each do |link| %>
						<div class="row">
      				<div class="col-12">
								<%= link_to link.url, link.url %>
							</div>
						</div>
					<% end %>
				</div>

				<div class="venue-text pad10-top pad10-bottom">
					<%= link_to 'Favorite', user_favorites_path(venue_id: @venue.id), method: :post, class: 'venue-text favorite-link' %>
				</div>
		
      </div>
    </div>
  </div>
</nav>